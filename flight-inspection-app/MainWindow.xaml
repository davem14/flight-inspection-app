<Window x:Class="flight_inspection_app.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:flight_inspection_app"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="900" Closing="Closing_Window">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="3.2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="1.4*"/>
        </Grid.RowDefinitions>
        <Grid x:Name="playBar" Grid.Row="3" Grid.Column="1"/>
        <Grid x:Name="details" Grid.Row="2" Grid.Column="0" Grid.RowSpan="2"/>
        <Grid x:Name="graph" Grid.Row="0" Grid.Column="1" Grid.RowSpan="3"/>
        <Grid x:Name="detector" Grid.Row="0" Grid.Column="0"/>
        <Grid x:Name="joystick" Grid.Row="1" />


    </Grid>
</Window>
